import React from 'react'
import ReactDOM from 'react-dom/client'
import GodTierTracker from './GodTierTracker.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GodTierTracker />
  </React.StrictMode>,
)
